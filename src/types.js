/**
 * Type definitions for Chrome Browser MCP Server
 */

export const ConnectionState = {
  disconnected: 'disconnected',
  connecting: 'connecting',
  connected: 'connected',
  error: 'error',
};

// Browser manager configuration schema
export const BrowserConfig = null; // Just a documentation type

// Page information schema
export const PageInfo = null;

// Snapshot element schema
export const SnapshotElement = null;

// Accessibility snapshot schema
export const AccessibilitySnapshot = null;

// Network request schema
export const NetworkRequest = null;

// Console message schema
export const ConsoleMessage = null;

// Cookie information schema
export const CookieInfo = null;

// Fill operation options
export const FillOptions = null;

// Wait options
export const WaitOptions = null;

// Navigation options
export const NavigateOptions = null;

// Emulation options
export const EmulateOptions = null;

// Screenshot options
export const ScreenshotOptions = null;

// Evaluate result
export const EvaluateResult = null;

// Browser manager instance
export const BrowserManager = null;